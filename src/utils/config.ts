export const API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL ?? ""
export const APP_SITE_URL = process.env.NEXT_PUBLIC_APP_SITE_URL ?? ""

/** Public LinkedIn app id (secret stays on the API). Override with NEXT_PUBLIC_LINKEDIN_CLIENT_ID. */
export const LINKEDIN_CLIENT_ID = process.env.NEXT_PUBLIC_LINKEDIN_CLIENT_ID ?? ''

/** Public Meta / Facebook app id (secret stays on the API). Override with NEXT_PUBLIC_META_APP_ID. */
export const META_APP_ID = process.env.NEXT_PUBLIC_META_APP_ID ?? ''