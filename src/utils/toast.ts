/**
 * Central re-export for Sonner toast.
 * Use: import toast from '@/utils/toast' or import { toast } from '@/utils/toast'
 * Toaster is rendered in AppProviders.
 */
export { toast as default, toast, Toaster } from 'sonner';
